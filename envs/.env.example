# Staging deploy – copy to .env.stg and fill in.
# Used by: scripts/deploy/deploy-to-stage.sh

# FTP (use SERWER_HOST or SERVER_HOST)
SERVER_HOST=ftp.example.com
USER=ftp_user
PASS=ftp_password
# Path on server where wp/ contents will be uploaded (no trailing slash)
REMOTE_PATH=/public_html

# wp-config.php – DB credentials for staging
DB_NAME=stg_dbname
DB_USER=stg_dbuser
DB_PASSWORD=stg_dbpass

# Base URL of staging site (no trailing slash) – for migration endpoint
SITE_URL=https://stage.example.com

# Optional: Basic auth for POST /wp-json/wi-calc/v1/migrations/run (WordPress admin + Application Password)
# MIGRATION_AUTH=admin_user:xxxx xxxx xxxx xxxx
