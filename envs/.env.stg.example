# Staging deploy – copy to .env.stg and fill in.
# Used by: scripts/deploy/deploy.sh (./deploy .env.stg)

# SFTP (use SERWER_HOST or SERVER_HOST)
SERVER_HOST=ftp.apflota.pl
USER=user@partnerapflota.pl
PASS=your_password
# SFTP port (default 22; many hosts use 22222)
SFTP_PORT=22222
# Path on server – relative to home (no leading /). Full path will be: ~/public_html/testy/...
# Example: public_html/testy → /home/damian.tokarczyk/public_html/testy/wp-content/themes/wi
REMOTE_PATH=public_html/testy

# wp-config.php – DB credentials for staging
DB_NAME=stg_dbname
DB_USER=stg_dbuser
DB_PASSWORD=stg_dbpass

# Base URL of staging site (no trailing slash) – for migration endpoint
SITE_URL=https://stage.example.com

# Optional: Basic auth for POST /wp-json/wi-calc/v1/migrations/run (WordPress admin + Application Password)
# MIGRATION_AUTH=admin_user:xxxx xxxx xxxx xxxx
